# 项目治理

本项目采用维护者审核、Issue 认领和 PR 合并的协作方式。翻译正确性、许可证和发布
状态不能由模型投票或单一自动检查决定。

## 决策范围

- 翻译表达：语言审校者决定，数学含义问题需要数学审校者；
- 术语：术语维护者通过独立 `term/*` PR 决定；
- Schema、工具和 CI：工具维护者审核，并提供迁移与测试；
- 英文上游同步：维护者审核来源 commit、stale 分类和重新审校；
- 发布：发布维护者确认全部门禁和许可证；
- 项目政策：维护者通过独立规范 PR 修改。

## 决策原则

1. 优先保护来源可追踪性、数学含义和审校记录；
2. 机器可读政策和人类文档必须同时更新；
3. 有不可逆影响、许可证不确定或审校身份不明时暂停；
4. 翻译、术语、工具、同步和发布使用独立 PR；
5. 公开异议记录在 Issue/PR 中，不依赖私人聊天作为唯一决定依据。

## 审批

普通文档和工具 PR 至少一名维护者批准。翻译和审校批准规则见
`docs/review-and-qa.md`；术语见 `docs/terminology.md`；发布见
`docs/release.md`。

GitHub 不允许 PR 作者批准自己的 PR。当前仓库作者 `@dongyaotalk` 是仓库管理员，
已配置为只能在 PR 上使用管理员 bypass：作者可以独立决定是否合并，不需要额外
维护者；但不能直接向 `main` 推送。管理员 bypass 不等同于人工语言或数学审校，
若发布门禁要求审校，仍须在 PR 或 `review/` 记录中明确决定、范围和理由。

其他贡献者的 PR 仍按普通维护者审批和 CODEOWNER 规则处理。未来加入维护者团队
后，可移除个人 bypass 并改由团队审核。

## 维护者变更

维护者列表位于 `MAINTAINERS.md`。新增或移除维护者使用独立治理 PR，说明职责、
GitHub 账号和生效日期。移除访问权限时还要轮换相关令牌并更新 CODEOWNERS。

治理文件不授予 GitHub 权限；实际权限以 GitHub 仓库设置为准。
